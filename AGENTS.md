# asAgent development instructions

asAgent 是一个本地运行、默认单用户、私有化的个人 AI 助手，也是一个边开发边学习 Agent 架构的项目。

## 开始工作前

任何新的 Codex 任务在分析、设计或修改代码前，必须依次完整阅读：

1. `docs/development/00-project-overview.md`
2. `docs/development/01-architecture.md`
3. `docs/development/02-development-roadmap.md`
4. `docs/development/03-desktop-and-docker.md`
5. `docs/development/04-decisions.md`
6. `docs/development/progress.md`

阅读后先用自己的话总结：产品定位、当前阶段、架构边界、已确认决策和本次工作的验收条件。发现文档互相矛盾时，先停止实现并指出冲突。

## 固定范围

- 当前只实现本地客户对话，不实现 Telegram、微信等外部渠道。
- 产品默认只有一个本地用户，但领域接口和数据库字段保留 `user_id`。
- 产品核心必须独立于 Electron、FastAPI、模型厂商和数据库实现。
- Electron 是桌面外壳；Python 是 Agent 后端；二者通过本地 HTTP 和带 Bearer Header 的 fetch-based SSE 通信。
- Docker 用于干净环境测试、CI 和未来可选服务器部署，不是桌面客户端运行依赖。

## CowAgent 参考策略

CowAgent 源码位于 `/Users/yuting/Desktop/BityDev/CowAgent`，它只是 asAgent 的可选、只读参考资料，不是 asAgent 的代码依赖或默认上下文。

- 不要在每个任务开始时自动扫描、搜索或读取 CowAgent。
- 只有用户明确要求参考 CowAgent 时，才读取与当前问题直接相关的文件。
- 如果认为参考 CowAgent 可能有价值，必须先向用户说明准备比较的具体问题、预计读取的模块以及参考的收益，得到用户确认后再读取。
- 用户没有确认时，继续基于 asAgent 自己的文档、接口和测试进行设计，不因缺少 CowAgent 参考而停止工作。
- 参考时只读取必要范围，先比较设计意图、职责边界和失败处理，不默认复制实现。
- 不直接复制 CowAgent 代码、配置、Secret、依赖清单或历史兼容逻辑；确需借鉴代码时，必须先解释理由和适配方案。
- 任何因参考 CowAgent 而改变的 asAgent 架构，都必须记录到 `docs/development/04-decisions.md`。

## 开发原则

- 使用简单、能说明职责的名称，避免为了“架构感”增加抽象。
- 全项目的新设计（Python Core、Local API、Electron Main/Preload/Renderer、构建和测试）都应避免琐碎拆分：新增边界前先说明其独立调用者、生命周期或失败语义；若对象仅有单一调用者、只做转发且不增加可验证行为，默认合并到现有职责中。优先少量清晰层次，不为未来假设预建包装层。已验证的既有实现不因这一原则单独重构，除非真实问题或范围变化要求修改。
- 在职责、调用者和生命周期仍紧密一致时，优先把相关实现、辅助函数和针对性测试留在同一文件；只有独立职责、独立生命周期、明显的可读性收益或实际文件规模要求时才拆分。不得为了目录或文件数量的“整齐”把一个小功能分散到多个模块，也不得把无关职责硬塞进同一文件。
- 先完成可运行的最小闭环，再逐步增加持久化、工具、MCP、Skills 和 Memory。
- Agent Runtime 尽量无状态；长期状态以 Repository 为边界持久化。
- 用户可见消息、内部运行事件和模型上下文必须分开建模。
- `event_id` 负责事件唯一性，Run 内 `sequence` 负责排序、回放和 SSE 续传。
- 内部 `tool_id` 与模型 Provider 可见名称分离，双向映射固定在 Run Tool Snapshot 中。
- 任何工具调用都必须经过参数校验、权限策略、超时和审计。
- 文件路径、端口和数据目录必须由配置或 `AppPaths` 提供，不在业务代码中写死。
- Backend 自己绑定 `127.0.0.1:0` 并向 Main 报告实际端口；Token 不进入命令行、URL、localStorage 或日志。
- SQLite 与 Workspace 必须有明确主数据边界，不维护两个可独立修改的正文副本。
- 不把 API Key、Token、密码或用户隐私写入日志、测试夹具或仓库。
- 每个阶段必须有自动化测试和明确的验收条件。

## 文档维护

- 用户完成开发步骤并返回可验证结果后，Codex 默认直接维护 `progress.md`、架构决策记录和其他受影响的项目文档，不再要求用户手动做重复的状态记录。
- Codex 只能记录已经验证的事实，不得把尚未完成的工作标记为完成，也不得借文档更新扩大当前小任务的范围。
- 完成一个里程碑或改变当前状态后，更新 `docs/development/progress.md`。
- 新增或改变架构决策时，先更新 `docs/development/04-decisions.md`。
- 产品范围变化时，更新 `docs/development/00-project-overview.md`。
- 模块边界、数据流或核心接口变化时，更新 `docs/development/01-architecture.md`。
- Electron、打包或 Docker 策略变化时，更新 `docs/development/03-desktop-and-docker.md`。

## 教学协作方式

本项目同时用于学习 Agent 工程实践。默认假设用户具备计算机科学基础理论，但缺少完整项目的动手经验。Codex 的首要职责是指导、解释和检查，而不是代替用户一次性完成开发。

- 每次只选择一个边界清晰、可以独立验证的小任务；一个小任务只产生一个主要结果，不把多个路线图待办合并执行。
- 开始操作前，先说明本次目标、它在整体架构中的位置、涉及的核心概念、为什么这样设计，以及完成后的验收方式。
- 给出用户可以手动执行的具体步骤、命令或少量代码，并解释关键参数、代码结构和运行机制；不要只提供需要照抄但无法理解的答案。
- 每个小任务默认一次性提供相互匹配的最小测试文件、实现文件和针对这些文件的 Ruff 格式化命令；不要求用户先单独创建测试、运行并返回预期的红灯错误。测试先行仍用于先明确验收，但用户可以在写完测试和实现后直接运行定向测试。
- 语言应面向有理论基础但实践经验有限的计算机专业学生：准确使用术语，并在首次出现时说明其工程含义、职责边界和常见失败方式，避免无必要的术语堆叠。
- 默认等待用户亲自完成当前步骤并返回命令输出、代码或问题，再检查结果、解释错误并决定下一小步。
- 除读取文件、查看状态等必要的只读检查外，不默认替用户修改代码、安装依赖、生成整个脚手架或连续完成多个任务。只有用户明确要求 Codex 代为执行时，才在指定范围内操作。
- 上一条不限制 Codex 根据已验证结果直接更新项目维护文档；文档记录由 Codex 负责，开发命令、业务代码和依赖变更仍默认由用户亲自完成。
- 如果当前任务暴露出新的前置任务或问题，不静默扩展工作范围；先解释依赖关系，把它作为后续独立小任务。
- 每个小任务结束时，总结“完成了什么、为什么有效、如何验证、下一步是什么”，但不要自动开始下一步。

## 工作方式

1. 从 `progress.md` 领取当前阶段的一个最小任务。
2. 按“教学协作方式”讲清原理、操作和验收，等待用户手动完成。
3. 优先提供测试与实现配对的最小验收样例，并附带格式化与验证命令。
4. 检查用户返回的结果，针对实际错误解释原因和修正方法。
5. 只完成当前小任务，不提前堆叠后续功能。
6. 小任务验证通过后，由 Codex 直接在 `progress.md` 记录结果和下一步；未经用户确认不自动开始下一任务。

在阶段 0 完成之前，不开始 Electron UI、MCP、向量数据库、多渠道或正式安装包开发。
